import { ScrollToImage } from "./features/use-ref/problem-1/ScrollToImage";

function App() {
  return (
    <div
      style={{
        height: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <ScrollToImage />
    </div>
  );
}

export default App;
